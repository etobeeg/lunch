name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: check if file exits
        run: |
          test -f ./test.txt && echo 'File exists'
          test -f ./test.txts && echo 'File do not exists'
      


        


